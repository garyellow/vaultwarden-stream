# Usage: cp .env.example .env && docker compose up -d

services:
  vaultwarden:
    image: ghcr.io/garyellow/vaultwarden-stream:latest
    container_name: vaultwarden
    restart: unless-stopped
    stop_grace_period: 300s
    env_file: .env
    ports:
      - "80:80"
